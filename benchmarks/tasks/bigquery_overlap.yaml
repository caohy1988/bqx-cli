# BigQuery parity tasks — dcx vs bq CLI.
#
# Placeholders like {project} are resolved from manifest.yaml at runtime.

tasks:
  # ── Read tasks ───────────────────────────────────────────────────────

  - id: bq-dataset-list
    track: parity
    system: bigquery
    dataset_tier: small
    goal: List all datasets in the project
    cli_variants:
      - name: dcx
        command: >-
          dcx datasets list
          --project-id {project}
          --format json
      - name: bq
        command: >-
          bq ls --format=json --project_id={project}
    validation:
      type: json_keys
      expected_keys: [datasets]
    metrics: &standard_metrics
      - exit_code
      - wall_clock_ms
      - stdout_bytes
      - stderr_bytes
      - token_input_estimate
      - token_output_estimate

  - id: bq-dataset-get
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Get metadata for a specific dataset
    cli_variants:
      - name: dcx
        command: >-
          dcx datasets get
          --project-id {project}
          --dataset-id {bq_benchmark_dataset}
          --format json
      - name: bq
        command: >-
          bq show --format=json {project}:{bq_benchmark_dataset}
    validation:
      type: json_keys
      expected_keys: [datasetReference]
    metrics: *standard_metrics

  - id: bq-table-list
    track: parity
    system: bigquery
    dataset_tier: small
    goal: List tables in a dataset
    cli_variants:
      - name: dcx
        command: >-
          dcx tables list
          --project-id {project}
          --dataset-id {bq_benchmark_dataset}
          --format json
      - name: bq
        command: >-
          bq ls --format=json {project}:{bq_benchmark_dataset}
    validation:
      type: json_keys
      expected_keys: [tables]
    metrics: *standard_metrics

  - id: bq-table-get
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Fetch schema for a known table
    cli_variants:
      - name: dcx
        command: >-
          dcx tables get
          --project-id {project}
          --dataset-id {bq_benchmark_dataset}
          --table-id {bq_table_narrow}
          --format json
      - name: bq
        command: >-
          bq show --schema --format=json {project}:{bq_benchmark_dataset}.{bq_table_narrow}
    validation:
      type: json_keys
      expected_keys: [schema]
    metrics: *standard_metrics

  # ── Query tasks ──────────────────────────────────────────────────────

  - id: bq-dryrun-aggregate
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Dry-run an aggregate query to validate SQL and estimate cost
    cli_variants:
      - name: dcx
        command: >-
          dcx jobs query
          --project-id {project}
          --sql "SELECT word, SUM(word_count) AS total FROM \`bigquery-public-data.samples.shakespeare\` GROUP BY word ORDER BY total DESC LIMIT 10"
          --dry-run
          --format json
      - name: bq
        command: >-
          bq query --dry_run --format=json --use_legacy_sql=false
          "SELECT word, SUM(word_count) AS total FROM \`bigquery-public-data.samples.shakespeare\` GROUP BY word ORDER BY total DESC LIMIT 10"
    validation:
      type: json_keys
      expected_keys: [statistics]
    metrics: *standard_metrics

  - id: bq-query-aggregate
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Execute an aggregate query and return results
    cli_variants:
      - name: dcx
        command: >-
          dcx jobs query
          --project-id {project}
          --sql "SELECT word, SUM(word_count) AS total FROM \`bigquery-public-data.samples.shakespeare\` GROUP BY word ORDER BY total DESC LIMIT 10"
          --format json
      - name: bq
        command: >-
          bq query --format=json --use_legacy_sql=false
          "SELECT word, SUM(word_count) AS total FROM \`bigquery-public-data.samples.shakespeare\` GROUP BY word ORDER BY total DESC LIMIT 10"
    validation:
      type: semantic_sql_result
    metrics: *standard_metrics

  - id: bq-query-nested
    track: parity
    system: bigquery
    dataset_tier: nested
    goal: Query nested fields from a semi-structured table
    cli_variants:
      - name: dcx
        command: >-
          dcx jobs query
          --project-id {project}
          --sql "SELECT author.name, COUNT(*) AS commits FROM \`bigquery-public-data.github_repos.commits\` GROUP BY author.name ORDER BY commits DESC LIMIT 10"
          --format json
      - name: bq
        command: >-
          bq query --format=json --use_legacy_sql=false
          "SELECT author.name, COUNT(*) AS commits FROM \`bigquery-public-data.github_repos.commits\` GROUP BY author.name ORDER BY commits DESC LIMIT 10"
    validation:
      type: semantic_sql_result
    metrics: *standard_metrics

  # ── Error-injection tasks ────────────────────────────────────────────

  - id: bq-error-malformed-sql
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Handle a malformed SQL query gracefully
    error_class: bad_sql
    expected_recovery_action: fix_input
    cli_variants:
      - name: dcx
        command: >-
          dcx jobs query
          --project-id {project}
          --sql "SELEKT * FORM shakespeare"
          --format json
      - name: bq
        command: >-
          bq query --format=json --use_legacy_sql=false
          "SELEKT * FORM shakespeare"
    validation:
      type: exit_code_only
      expected_exit_code_nonzero: true
    metrics: *standard_metrics

  - id: bq-error-not-found
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Handle a request for a nonexistent dataset
    error_class: not_found
    expected_recovery_action: fix_input
    cli_variants:
      - name: dcx
        command: >-
          dcx datasets get
          --project-id {project}
          --dataset-id {bq_nonexistent_dataset}
          --format json
      - name: bq
        command: >-
          bq show --format=json {project}:{bq_nonexistent_dataset}
    validation:
      type: exit_code_only
      expected_exit_code_nonzero: true
    metrics: *standard_metrics

  - id: bq-error-auth-failure
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Handle an authentication failure
    error_class: auth
    expected_recovery_action: reauth
    cli_variants:
      - name: dcx
        command: >-
          dcx datasets list
          --project-id {project}
          --token invalid-token-value
          --format json
      - name: bq
        command: >-
          bq ls --format=json --project_id={project}
          --credential_file=/dev/null
    validation:
      type: exit_code_only
      expected_exit_code_nonzero: true
    metrics: *standard_metrics

  - id: bq-error-invalid-flag
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Handle an invalid flag gracefully
    error_class: invalid_flag
    expected_recovery_action: fix_input
    cli_variants:
      - name: dcx
        command: >-
          dcx datasets list
          --project-id {project}
          --nonexistent-flag value
          --format json
      - name: bq
        command: >-
          bq ls --format=json --nonexistent_flag=value
    validation:
      type: exit_code_only
      expected_exit_code_nonzero: true
    metrics: *standard_metrics

  - id: bq-error-permission-denied
    track: parity
    system: bigquery
    dataset_tier: small
    goal: Handle a permission denied error
    error_class: permission_denied
    expected_recovery_action: stop
    cli_variants:
      - name: dcx
        command: >-
          dcx datasets list
          --project-id bigquery-public-data
          --format json
      - name: bq
        command: >-
          bq ls --format=json --project_id=bigquery-public-data
    validation:
      type: exit_code_only
      expected_exit_code_nonzero: true
    metrics: *standard_metrics
