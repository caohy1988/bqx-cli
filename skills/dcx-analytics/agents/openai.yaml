interface:
  display_name: "dcx Analytics"
  short_description: "Agent health checks, session evaluation, trace debugging, and drift detection"
  default_prompt: "Use $dcx-analytics for agent analytics workflows — doctor, evaluate, get-trace, drift detection, and event views."

policy:
  allow_implicit_invocation: true
