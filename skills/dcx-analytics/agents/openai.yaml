interface:
  display_name: "dcx Analytics"
  short_description: "Route to doctor, evaluate, or get-trace"
  default_prompt: "Use $dcx-analytics to check agent health and find failing sessions in BigQuery."

policy:
  allow_implicit_invocation: true
