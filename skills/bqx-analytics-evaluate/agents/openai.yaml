interface:
  display_name: "bqx Evaluate"
  short_description: "Evaluate sessions against thresholds"
  default_prompt: "Use $bqx-analytics-evaluate to check agent session latency or error rates against a threshold."

policy:
  allow_implicit_invocation: true
