interface:
  display_name: "bqx Datasets"
  short_description: "get/list BigQuery datasets via bqx"
  default_prompt: "Use $bqx-datasets to get/list BigQuery datasets using the bqx CLI."

policy:
  allow_implicit_invocation: true
