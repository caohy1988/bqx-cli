interface:
  display_name: "dcx Connections"
  short_description: "Inspect BigQuery external connections via dcx"
  default_prompt: "Use $dcx-connections to inspect BigQuery external connection metadata using INFORMATION_SCHEMA queries."

policy:
  allow_implicit_invocation: true
