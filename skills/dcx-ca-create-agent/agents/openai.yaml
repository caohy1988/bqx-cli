interface:
  display_name: "dcx CA Create Agent"
  short_description: "Create a Conversational Analytics data agent"
  default_prompt: "Use $dcx-ca-create-agent to create a data agent with tables, views, and verified queries."

policy:
  allow_implicit_invocation: true
