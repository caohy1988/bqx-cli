interface:
  display_name: "dcx Drift Detection"
  short_description: "Detect behavioral drift in agent sessions"
  default_prompt: "Use $dcx-analytics-drift to compare agent metrics across time windows and detect performance regressions."

policy:
  allow_implicit_invocation: true
