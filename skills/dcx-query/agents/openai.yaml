interface:
  display_name: "dcx Query"
  short_description: "Run raw BigQuery SQL through dcx"
  default_prompt: "Use $dcx-query to execute a SQL query against BigQuery using the dcx CLI."

policy:
  allow_implicit_invocation: true
