interface:
  display_name: "dcx Conversational Analytics"
  short_description: "Route to CA ask, create-agent, or list-agents across Data Cloud sources"
  default_prompt: "Use $dcx-ca to ask natural language questions over Data Cloud sources (BigQuery, Looker, AlloyDB, Spanner, Cloud SQL) or manage data agents."

policy:
  allow_implicit_invocation: true
