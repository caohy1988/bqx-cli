interface:
  display_name: "bqx Trace"
  short_description: "Inspect a session's event trace"
  default_prompt: "Use $bqx-analytics-trace to retrieve and debug the event trace for a specific agent session."

policy:
  allow_implicit_invocation: true
