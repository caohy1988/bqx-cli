interface:
  display_name: "bqx CA Ask"
  short_description: "Ask natural language questions over BigQuery data"
  default_prompt: "Use $bqx-ca-ask to translate a natural language question into SQL and run it against BigQuery."

policy:
  allow_implicit_invocation: true
