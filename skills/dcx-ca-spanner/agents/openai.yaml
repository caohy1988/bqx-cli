interface:
  display_name: "dcx CA Spanner"
  short_description: "Spanner-specific CA setup and business query patterns"
  default_prompt: "Use $dcx-ca-spanner to set up and use Conversational Analytics for Spanner databases."

policy:
  allow_implicit_invocation: true
